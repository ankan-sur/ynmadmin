import type { AmbientContext } from "./AmbientContext";
import type { ActionTrigger, ActionExecutionScope } from "./types";
/** Context of the `scheduledShopifySync` action. */
export interface ScheduledShopifySyncGlobalActionContext extends AmbientContext {
    /**
    * @deprecated Use 'returnType' instead.
    * Useful for returning data from this action by setting `scope.result`.
    */
    scope: ActionExecutionScope;
    /**
    * An object specifying the trigger to this action (e.g. API call, custom params).
    */
    params: {};
    /**
    * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
    */
    trigger: ActionTrigger;
    /**
    * @private The context of this action.
    */
    context: ScheduledShopifySyncGlobalActionContext;
}
/** Context of the `smartCollect` action. */
export interface SmartCollectGlobalActionContext extends AmbientContext {
    /**
    * @deprecated Use 'returnType' instead.
    * Useful for returning data from this action by setting `scope.result`.
    */
    scope: ActionExecutionScope;
    /**
    * An object specifying the trigger to this action (e.g. API call, custom params).
    */
    params: {};
    /**
    * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
    */
    trigger: ActionTrigger;
    /**
    * @private The context of this action.
    */
    context: SmartCollectGlobalActionContext;
}
/** Context of the `setTotalZero` action. */
export interface SetTotalZeroGlobalActionContext extends AmbientContext {
    /**
    * @deprecated Use 'returnType' instead.
    * Useful for returning data from this action by setting `scope.result`.
    */
    scope: ActionExecutionScope;
    /**
    * An object specifying the trigger to this action (e.g. API call, custom params).
    */
    params: {};
    /**
    * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
    */
    trigger: ActionTrigger;
    /**
    * @private The context of this action.
    */
    context: SetTotalZeroGlobalActionContext;
}
/** Context of the `test2` action. */
export interface Test2GlobalActionContext extends AmbientContext {
    /**
    * @deprecated Use 'returnType' instead.
    * Useful for returning data from this action by setting `scope.result`.
    */
    scope: ActionExecutionScope;
    /**
    * An object specifying the trigger to this action (e.g. API call, custom params).
    */
    params: {
        tag?: string;
    };
    /**
    * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
    */
    trigger: ActionTrigger;
    /**
    * @private The context of this action.
    */
    context: Test2GlobalActionContext;
}
/** Context of the `test3` action. */
export interface Test3GlobalActionContext extends AmbientContext {
    /**
    * @deprecated Use 'returnType' instead.
    * Useful for returning data from this action by setting `scope.result`.
    */
    scope: ActionExecutionScope;
    /**
    * An object specifying the trigger to this action (e.g. API call, custom params).
    */
    params: {};
    /**
    * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
    */
    trigger: ActionTrigger;
    /**
    * @private The context of this action.
    */
    context: Test3GlobalActionContext;
}
/** Context of the `fetchEmails` action. */
export interface FetchEmailsGlobalActionContext extends AmbientContext {
    /**
    * @deprecated Use 'returnType' instead.
    * Useful for returning data from this action by setting `scope.result`.
    */
    scope: ActionExecutionScope;
    /**
    * An object specifying the trigger to this action (e.g. API call, custom params).
    */
    params: {};
    /**
    * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
    */
    trigger: ActionTrigger;
    /**
    * @private The context of this action.
    */
    context: FetchEmailsGlobalActionContext;
}
/** Context of the `smartCollecter` action. */
export interface SmartCollecterGlobalActionContext extends AmbientContext {
    /**
    * @deprecated Use 'returnType' instead.
    * Useful for returning data from this action by setting `scope.result`.
    */
    scope: ActionExecutionScope;
    /**
    * An object specifying the trigger to this action (e.g. API call, custom params).
    */
    params: {};
    /**
    * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
    */
    trigger: ActionTrigger;
    /**
    * @private The context of this action.
    */
    context: SmartCollecterGlobalActionContext;
}
/** Context of the `fetchUsers` action. */
export interface FetchUsersGlobalActionContext extends AmbientContext {
    /**
    * @deprecated Use 'returnType' instead.
    * Useful for returning data from this action by setting `scope.result`.
    */
    scope: ActionExecutionScope;
    /**
    * An object specifying the trigger to this action (e.g. API call, custom params).
    */
    params: {};
    /**
    * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
    */
    trigger: ActionTrigger;
    /**
    * @private The context of this action.
    */
    context: FetchUsersGlobalActionContext;
}
/** Context of the `test` action. */
export interface TestGlobalActionContext extends AmbientContext {
    /**
    * @deprecated Use 'returnType' instead.
    * Useful for returning data from this action by setting `scope.result`.
    */
    scope: ActionExecutionScope;
    /**
    * An object specifying the trigger to this action (e.g. API call, custom params).
    */
    params: {
        key?: string;
    };
    /**
    * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
    */
    trigger: ActionTrigger;
    /**
    * @private The context of this action.
    */
    context: TestGlobalActionContext;
}
